<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LagalLens">
        <activity
            android:name=".presentation.feature.upload.error.ui.UploadErrorActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.feature.upload.progress.ui.UploadProgressActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.feature.upload.ui.UploadSourceActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.feature.ocr.ui.OcrReviewActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presentation.feature.splash.ui.SplashActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".presentation.feature.onboarding.ui.OnboardingActivity"
            android:exported="false" />
    </application>

</manifest>
